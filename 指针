
一、指针计算：
sizeof(char)=1   sizeof(int)=4
当指针加一的时候不是在地址值上加一而是加sizeof。

给指针加、减一个整数（+，-，+=，-=）。
递增递减（++、--）。
两个指针可以相加减。（不是地址的差，给的是地址差除以sizeof）。

++后缀输出的是值++之前的值。

二、指针作用
需要传入较大的数据时用作参数。
传入数组后对数组做操作。
函数返回不止一个结果。
需要用函数来修改不止一个变量。
动态申请的内存。
